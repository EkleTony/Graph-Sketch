# Saving the requested annotated comment to a .txt file

multi_layer_tensor_txt = """
// -----------------------------------------------------------------------------
// ✅ CHANGES FOR MULTI-LAYER (TENSOR) SKETCHING
// -----------------------------------------------------------------------------

/*
1. INCLUDED HEADER:
   #include "GraphSketchTensor.hpp"

   - This brings in the new SketchTensorManager class that handles 
     time-window × protocol-based Count-Min Sketches (CMSCU).

2. REPLACED SKETCH INITIALIZATION:
   // OLD:
   GraphSketch::GraphSketchFiltering sketch(5, 3000, 0.90f, 0.95f);
   
   // NEW:
   GraphSketch::SketchTensorManager sketchTensor(4, 2000, 0.95f, 1.0f);

   - r = 4 hash functions
   - c = 2000 columns
   - decayFactor = 0.95
   - updateStep = 1.0

3. MODIFIED MAIN LOOP:
   - For each edge:
     a) Assign a dummy protocol ID (e.g., 0 for TCP)
     b) Use sketchTensor.addEdge() to route the edge
     c) Use sketchTensor.estimateScore() to get anomaly score

   - Example:
     int protocolID = 0;
     sketchTensor.addEdge(src, dst, timestamp, protocolID);
     float anomalyScore = sketchTensor.estimateScore(src, dst, timestamp, protocolID);

4. ADDED DECAY + PRUNE:
   - Every 10,000 edges:
     sketchTensor.applyDecay();
     sketchTensor.pruneOldWindows(timestamp / 300);

   - This:
     ✅ Keeps old time windows from accumulating in memory
     ✅ Applies temporal smoothing to sketch values

5. KEPT EVALUATION CODE UNCHANGED:
   - You can still use your existing Python script 
     to compute AUC, Precision@K, etc.

6. NEXT STEP (Optional):
   - Replace dummy `protocolID = 0` with actual protocol parsing if your dataset includes it.
   - Consider fusing multiple sketch scores (e.g., max over recent windows).

7. OVERALL BENEFITS:
   ✅ Increased precision via localized sketches
   ✅ Supports real-time anomaly detection per context
   ✅ Scalable and ready for GPU or streaming integration

*/
"""

with open("/mnt/data/MultiLayerTensorSketching_Notes.txt", "w") as file:
    file.write(multi_layer_tensor_txt)
